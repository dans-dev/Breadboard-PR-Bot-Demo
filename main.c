#include <stdio.h>

int main() {
    // TODO: build a linked-list version of this array
    int numbers[] = {1, 2, 3, 4, 5};

    for (int i = 0; i < 5; i++) {
        printf("%d\n", numbers[i]);
    }

    return 0;
}

